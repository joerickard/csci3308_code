
create schema simplestore;

set search_path to simplestore, public;

create table simplestore.users(uid integer primary key, username text not null, password text not null);

create table simplestore.files(fid integer primary key, filename text not null, filepath text not null);

create table simplestore.permissions(pid integer primary key, fid integer not null, uid integer not null);

